<UserControl x:Class="EasySave_WPF.Views.CreateJobView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasySave_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" Text="Create New Backup Job" 
                   FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Formulaire -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Nom du job -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Job Name:" 
                       VerticalAlignment="Center" Margin="0,0,10,10" />
            <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" 
                     Text="{Binding NewJobName, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,10" Padding="5" />

            <!-- Chemin source -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Source Path:" 
                       VerticalAlignment="Center" Margin="0,0,10,10" />
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding NewJobSourcePath, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,10,10" Padding="5" />
            <Button Grid.Row="1" Grid.Column="2" Content="Browse..." 
                    Command="{Binding BrowseSourceCommand}" 
                    Width="80" Margin="0,0,0,10" />

            <!-- Chemin cible -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Target Path:" 
                       VerticalAlignment="Center" Margin="0,0,10,10" />
            <TextBox Grid.Row="2" Grid.Column="1" 
                     Text="{Binding NewJobTargetPath, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,10,10" Padding="5" />
            <Button Grid.Row="2" Grid.Column="2" Content="Browse..." 
                    Command="{Binding BrowseTargetCommand}" 
                    Width="80" Margin="0,0,0,10" />

            <!-- Type de sauvegarde -->
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Backup Type:" 
                       VerticalAlignment="Center" Margin="0,0,10,10" />
            <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal">
                <RadioButton Content="Complete" IsChecked="{Binding NewJobType, ConverterParameter=Complete, Converter={StaticResource RadioButtonCheckedConverter}}" 
                           Margin="0,0,20,0" VerticalContentAlignment="Center" />
                <RadioButton Content="Differential" IsChecked="{Binding NewJobType, ConverterParameter=Differential, Converter={StaticResource RadioButtonCheckedConverter}}" 
                           VerticalContentAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Boutons d'action -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" Command="{Binding NavigateCommand}" CommandParameter="Home" 
                    Width="100" Height="30" Margin="0,0,10,0" />
            <Button Content="Create Job" Command="{Binding CreateJobCommand}" 
                    Width="120" Height="30" Background="#27AE60" Foreground="White" />
        </StackPanel>
    </Grid>
</UserControl>
<UserControl x:Class="EasySave_WPF.Views.DecryptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasySave_WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chemin du fichier -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="FilePathTextBox" Width="320" IsReadOnly="True" Margin="0,0,10,0"/>
            <Button Content="Parcourir" Width="100" Click="BrowseButton_Click"/>
        </StackPanel>

        <!-- Bouton de déchiffrement -->
        <Button Grid.Row="1" Content="Déchiffrer" Width="150" Height="30" HorizontalAlignment="Center"
                Click="DecryptButton_Click"/>

        <!-- Message de confirmation -->
        <TextBlock x:Name="MessageTextBlock" Grid.Row="2" Foreground="Green" Margin="0,20,0,0"
                   TextAlignment="Center" Visibility="Collapsed"/>
    </Grid>
</UserControl>
